// problem number 1

function cubeNumber(number) {
    const frist = Math.pow(3, 3);
    const num = Math.pow(4, 3);
    console.log(frist);
    return num;
}
let myNumber = 27;
let mysecond = 64;
let cubeNumbers = cubeNumber(myNumber, mysecond);
console.log(cubeNumbers);